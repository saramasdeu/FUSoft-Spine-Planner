# Pseudo_CTS — dependencies
# Install with: pip install -r requirements.txt
#
# For CUDA (external server), install torch separately first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# For CPU/MPS (Mac):
#   pip install torch torchvision

monai>=1.0.0
SimpleITK>=2.1.0
numpy>=1.22.0
scikit-image>=0.19.0
torchio>=0.18.0